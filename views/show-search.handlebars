<main class="container">
  <div class="row d-flex justify-content-center">
    <h3 class="text-center text-warning">Search results for {{title}}</h3>
  </div>
  <div class="row d-flex justify-content-center">
    <div class="col-12 col-md-6">
      {{#each reviews}}
      {{>shows/show-review}}
      {{/each}}
    </div>
  </div>
</main>